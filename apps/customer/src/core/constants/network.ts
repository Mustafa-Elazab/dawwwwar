export const NETWORK_DEFAULTS = {
  apiPort: 3000,
  apiPrefix: '/api/v1',
  socketPort: 3000,
  timeoutMs: 15000,
  retryCount: 2,
  retryBackoffMs: 400,
};

export const DEV_HOSTS = {
  androidEmulator: '10.0.2.2',
  iosSimulator: 'localhost',
};
