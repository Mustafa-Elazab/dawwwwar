{
  "name": "@dawwar/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types for Dawwar apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "build": "echo 'types are source-only, no build step'"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
